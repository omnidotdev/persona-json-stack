services:
  # ----------------------------
  # service-name:
  #   repo: repository URL (required)
  #   path: local path (optional, defaults to `services/service-name`)
  #   env: array of environment variables to set (optional)
  # ----------------------------
  - persona-json-schema:
      repo: https://github.com/omnidotdev/persona-json-schema#master
  - persona-json-site:
      repo: https://github.com/omnidotdev/persona-json-site#master
